/* ============================================
   QRScanner.css
   تصميم: "سماء ليل الكنيسة" - خلفية ليلية متحركة
   كل الـ selectors هنا محصورة جوه .qr-scanner-optimized
   عشان متأثرش على أي صفحة تانية (نفس الطريقة المستخدمة
   في باقي صفحات الأدمن زي .manage-students-optimized)
   ============================================ */

@import url('https://fonts.googleapis.com/css2?family=Aref+Ruqaa:wght@400;700&family=Cairo:wght@400;500;600;700;800&display=swap');

/* ============================================
   ROOT / VARIABLES
   ============================================ */

.qr-scanner-optimized {
  --qr-midnight: #0B1026;
  --qr-midnight-deep: #060815;
  --qr-midnight-light: #161B3A;
  --qr-candle-gold: #F0B429;
  --qr-ember: #FF8C42;
  --qr-starlight: #E7ECFA;
  --qr-starlight-dim: rgba(231, 236, 250, 0.65);
  --qr-glass-bg: rgba(255, 255, 255, 0.06);
  --qr-glass-bg-strong: rgba(255, 255, 255, 0.1);
  --qr-glass-border: rgba(255, 255, 255, 0.14);
  --qr-violet: #A78BFA;
  --qr-emerald: #34D399;
  --qr-sky: #60A5FA;
  --qr-radius-lg: 20px;
  --qr-radius-md: 14px;
  --qr-radius-sm: 10px;
  --qr-shadow-glow: 0 8px 32px rgba(0, 0, 0, 0.4);

  position: relative;
  min-height: 100vh;
  background: var(--qr-midnight);
  color: var(--qr-starlight);
  font-family: 'Cairo', 'Tajawal', sans-serif;
  overflow-x: hidden;
  padding-bottom: 40px;
}

.qr-scanner-optimized * {
  box-sizing: border-box;
}

.qr-scanner-optimized h1,
.qr-scanner-optimized h2,
.qr-scanner-optimized h3 {
  font-family: 'Aref Ruqaa', 'Cairo', serif;
  margin: 0;
}

.qr-scanner-optimized button,
.qr-scanner-optimized input,
.qr-scanner-optimized select {
  font-family: inherit;
}

.qr-scanner-optimized button:focus-visible,
.qr-scanner-optimized input:focus-visible,
.qr-scanner-optimized select:focus-visible {
  outline: 2px solid var(--qr-candle-gold);
  outline-offset: 2px;
}

/* ============================================
   الخلفية المتحركة - نجوم + شرر صاعد
   ============================================ */

.qr-scanner-optimized .stars {
  position: fixed;
  inset: 0;
  z-index: 0;
  pointer-events: none;
  overflow: hidden;
  background:
    radial-gradient(ellipse 120% 60% at 50% -10%, rgba(167, 139, 250, 0.18), transparent 60%),
    linear-gradient(180deg, var(--qr-midnight-deep) 0%, var(--qr-midnight) 45%, #0E1330 100%);
}

.qr-scanner-optimized .stars::before {
  content: '';
  position: absolute;
  inset: -10%;
  background-image:
    radial-gradient(1.5px 1.5px at 20px 30px, #E7ECFA, transparent),
    radial-gradient(1.5px 1.5px at 90px 120px, #E7ECFA, transparent),
    radial-gradient(1px 1px at 160px 60px, #E7ECFA, transparent),
    radial-gradient(1.5px 1.5px at 210px 180px, #E7ECFA, transparent),
    radial-gradient(1px 1px at 260px 40px, #E7ECFA, transparent),
    radial-gradient(2px 2px at 320px 140px, #E7ECFA, transparent),
    radial-gradient(1px 1px at 30px 200px, #E7ECFA, transparent),
    radial-gradient(1.5px 1.5px at 130px 240px, #E7ECFA, transparent);
  background-repeat: repeat;
  background-size: 360px 280px;
  opacity: 0.7;
  animation: qr-stars-drift 90s linear infinite, qr-stars-twinkle 4s ease-in-out infinite alternate;
}

.qr-scanner-optimized .stars::after {
  content: '';
  position: absolute;
  inset: 0;
  background-image:
    radial-gradient(3px 3px at 10% 90%, rgba(240, 180, 41, 0.9), transparent 60%),
    radial-gradient(2.5px 2.5px at 25% 95%, rgba(255, 140, 66, 0.8), transparent 60%),
    radial-gradient(3px 3px at 45% 100%, rgba(240, 180, 41, 0.7), transparent 60%),
    radial-gradient(2px 2px at 65% 92%, rgba(255, 140, 66, 0.8), transparent 60%),
    radial-gradient(3px 3px at 80% 98%, rgba(240, 180, 41, 0.9), transparent 60%),
    radial-gradient(2.5px 2.5px at 95% 90%, rgba(255, 140, 66, 0.7), transparent 60%);
  filter: blur(0.5px);
  animation: qr-embers-rise 12s ease-in infinite;
  opacity: 0.85;
}

@keyframes qr-stars-drift {
  from { transform: translate3d(0, 0, 0); }
  to { transform: translate3d(-360px, -280px, 0); }
}

@keyframes qr-stars-twinkle {
  from { opacity: 0.45; }
  to { opacity: 0.85; }
}

@keyframes qr-embers-rise {
  0% { transform: translateY(0) scale(1); opacity: 0; }
  10% { opacity: 1; }
  90% { opacity: 0.6; }
  100% { transform: translateY(-100vh) scale(0.6); opacity: 0; }
}

@media (prefers-reduced-motion: reduce) {
  .qr-scanner-optimized .stars::before,
  .qr-scanner-optimized .stars::after,
  .qr-scanner-optimized .header-glass::before {
    animation: none;
  }
}

/* ============================================
   HEADER
   ============================================ */

.qr-scanner-optimized .smart-header {
  position: relative;
  z-index: 1;
  padding: 20px 16px 12px;
}

.qr-scanner-optimized .header-glass {
  position: relative;
  overflow: hidden;
  background: var(--qr-glass-bg);
  border: 1px solid var(--qr-glass-border);
  border-radius: var(--qr-radius-lg);
  padding: 18px 20px;
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  box-shadow: var(--qr-shadow-glow);
}

.qr-scanner-optimized .header-glass::before {
  content: '';
  position: absolute;
  top: -50%;
  inset-inline-end: -20%;
  width: 200px;
  height: 200px;
  background: radial-gradient(circle, rgba(240, 180, 41, 0.25), transparent 70%);
  animation: qr-halo-pulse 6s ease-in-out infinite;
  pointer-events: none;
}

@keyframes qr-halo-pulse {
  0%, 100% { opacity: 0.5; transform: scale(1); }
  50% { opacity: 1; transform: scale(1.15); }
}

.qr-scanner-optimized .header-row {
  position: relative;
  z-index: 1;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
}

.qr-scanner-optimized .greeting-section {
  flex: 1;
  min-width: 0;
}

.qr-scanner-optimized .greeting-section h1 {
  font-size: 21px;
  font-weight: 700;
  color: var(--qr-starlight);
  letter-spacing: 0.3px;
}

.qr-scanner-optimized .status-row {
  margin-top: 6px;
}

.qr-scanner-optimized .connection-status {
  font-size: 12px;
  color: var(--qr-starlight-dim);
}

.qr-scanner-optimized .connection-status.online {
  color: var(--qr-emerald);
}

.qr-scanner-optimized .header-actions {
  display: flex;
  align-items: center;
  gap: 8px;
}

/* ============================================
   أزرار عامة (رجوع، تحديث..)
   ============================================ */

.qr-scanner-optimized .action-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 40px;
  height: 40px;
  border-radius: 50%;
  border: 1px solid var(--qr-glass-border);
  background: var(--qr-glass-bg);
  color: var(--qr-starlight);
  font-size: 18px;
  cursor: pointer;
  transition: background 0.2s, transform 0.2s;
}

.qr-scanner-optimized .action-btn:hover:not(:disabled) {
  background: var(--qr-glass-bg-strong);
  transform: scale(1.05);
}

.qr-scanner-optimized .action-btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.qr-scanner-optimized .refresh-btn {
  width: 36px;
  height: 36px;
  font-size: 15px;
}

.qr-scanner-optimized .refresh-btn:hover:not(:disabled) {
  transform: rotate(180deg);
}

/* ============================================
   TOAST
   ============================================ */

.qr-scanner-optimized .toast-smart {
  position: fixed;
  top: 16px;
  left: 50%;
  transform: translateX(-50%);
  z-index: 100;
  padding: 12px 20px;
  border-radius: var(--qr-radius-md);
  background: var(--qr-midnight-light);
  border: 1px solid var(--qr-glass-border);
  color: var(--qr-starlight);
  box-shadow: var(--qr-shadow-glow);
  font-size: 14px;
  max-width: 90%;
  text-align: center;
  white-space: pre-line;
  animation: qr-toast-in 0.3s ease;
}

.qr-scanner-optimized .toast-smart.error {
  border-color: rgba(248, 113, 113, 0.5);
  background: linear-gradient(135deg, rgba(127, 29, 29, 0.9), var(--qr-midnight-light));
}

.qr-scanner-optimized .toast-smart.success {
  border-color: rgba(52, 211, 153, 0.5);
  background: linear-gradient(135deg, rgba(6, 78, 59, 0.9), var(--qr-midnight-light));
}

@keyframes qr-toast-in {
  from { opacity: 0; transform: translate(-50%, -12px); }
  to { opacity: 1; transform: translate(-50%, 0); }
}

/* ============================================
   PROCESSING OVERLAY
   ============================================ */

.qr-scanner-optimized .processing-overlay-smart {
  position: fixed;
  inset: 0;
  z-index: 90;
  background: rgba(6, 8, 21, 0.85);
  backdrop-filter: blur(6px);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 16px;
}

.qr-scanner-optimized .logo-loader {
  position: relative;
  width: 64px;
  height: 64px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.qr-scanner-optimized .logo-icon {
  font-size: 28px;
  z-index: 1;
}

.qr-scanner-optimized .logo-spinner {
  position: absolute;
  inset: 0;
  border: 3px solid rgba(240, 180, 41, 0.2);
  border-top-color: var(--qr-candle-gold);
  border-radius: 50%;
  animation: qr-spin 0.9s linear infinite;
}

@keyframes qr-spin {
  to { transform: rotate(360deg); }
}

.qr-scanner-optimized .loading-text {
  color: var(--qr-starlight-dim);
  font-size: 14px;
}

/* ============================================
   DASHBOARD BODY / SECTIONS
   ============================================ */

.qr-scanner-optimized .dashboard-body-smart {
  position: relative;
  z-index: 1;
  padding: 8px 16px 24px;
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.qr-scanner-optimized .section-smart {
  background: var(--qr-glass-bg);
  border: 1px solid var(--qr-glass-border);
  border-radius: var(--qr-radius-lg);
  padding: 18px;
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
}

.qr-scanner-optimized .section-title-smart {
  font-size: 16px;
  font-weight: 700;
  color: var(--qr-starlight);
  margin-bottom: 14px;
}

.qr-scanner-optimized .section-header-smart {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 14px;
}

.qr-scanner-optimized .section-header-smart .section-title-smart {
  margin-bottom: 0;
}

/* ============================================
   ACTIVITY GRID
   ============================================ */

.qr-scanner-optimized .activity-grid-smart {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 10px;
}

.qr-scanner-optimized .activity-card-smart {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6px;
  background: var(--qr-glass-bg);
  border: 1px solid var(--qr-glass-border);
  border-radius: var(--qr-radius-md);
  padding: 16px 8px;
  color: var(--qr-starlight);
  cursor: pointer;
  transition: transform 0.2s, border-color 0.2s, background 0.2s;
}

.qr-scanner-optimized .activity-card-smart:hover:not(:disabled) {
  transform: translateY(-3px);
  border-color: var(--qr-candle-gold);
  background: var(--qr-glass-bg-strong);
}

.qr-scanner-optimized .activity-card-smart:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.qr-scanner-optimized .activity-icon-smart {
  font-size: 28px;
}

.qr-scanner-optimized .activity-card-smart h3 {
  font-size: 13px;
  font-weight: 600;
  text-align: center;
}

.qr-scanner-optimized .activity-reward-smart {
  font-size: 12px;
  font-weight: 700;
  color: var(--qr-candle-gold);
}

/* ============================================
   SCANNER SECTION
   ============================================ */

.qr-scanner-optimized .scanner-section-smart {
  background: var(--qr-glass-bg);
  border: 1px solid var(--qr-glass-border);
  border-radius: var(--qr-radius-lg);
  padding: 18px;
  backdrop-filter: blur(16px);
}

.qr-scanner-optimized .scanner-header-smart {
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 8px;
  margin-bottom: 14px;
}

.qr-scanner-optimized .scanner-header-smart h2 {
  font-size: 16px;
  font-weight: 700;
}

.qr-scanner-optimized .cache-info {
  display: flex;
  align-items: center;
  gap: 6px;
  background: rgba(240, 180, 41, 0.15);
  border: 1px solid rgba(240, 180, 41, 0.3);
  padding: 6px 12px;
  border-radius: 999px;
}

.qr-scanner-optimized .info-icon {
  font-size: 14px;
}

.qr-scanner-optimized .info-text {
  font-size: 13px;
  font-weight: 600;
  color: var(--qr-candle-gold);
}

.qr-scanner-optimized .qr-reader-smart {
  border-radius: var(--qr-radius-md);
  overflow: hidden;
  margin-bottom: 14px;
}

.qr-scanner-optimized .btn-cancel-smart {
  width: 100%;
  padding: 12px;
  border-radius: var(--qr-radius-md);
  border: 1px solid rgba(248, 113, 113, 0.35);
  background: rgba(127, 29, 29, 0.25);
  color: #FCA5A5;
  font-weight: 600;
  font-size: 14px;
  cursor: pointer;
  transition: background 0.2s;
}

.qr-scanner-optimized .btn-cancel-smart:hover:not(:disabled) {
  background: rgba(127, 29, 29, 0.4);
}

.qr-scanner-optimized .btn-cancel-smart:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

/* ============================================
   DATE BROWSER (اعرض بيانات يوم)
   ============================================ */

.qr-scanner-optimized .date-browser-section-smart {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.qr-scanner-optimized .date-chips-smart {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.qr-scanner-optimized .chip-smart {
  padding: 8px 16px;
  border-radius: 999px;
  border: 1px solid var(--qr-glass-border);
  background: var(--qr-glass-bg);
  color: var(--qr-starlight-dim);
  font-size: 13px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s ease;
  white-space: nowrap;
}

.qr-scanner-optimized .chip-smart:hover {
  background: var(--qr-glass-bg-strong);
  color: var(--qr-starlight);
}

.qr-scanner-optimized .chip-smart.active {
  background: linear-gradient(135deg, var(--qr-candle-gold), var(--qr-ember));
  border-color: transparent;
  color: var(--qr-midnight-deep);
  box-shadow: 0 2px 14px rgba(240, 180, 41, 0.45);
}

.qr-scanner-optimized .date-picker-row-smart {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
}

.qr-scanner-optimized .date-input-smart,
.qr-scanner-optimized .activity-filter-smart {
  flex: 1;
  min-width: 140px;
  padding: 10px 12px;
  border-radius: var(--qr-radius-sm);
  border: 1px solid var(--qr-glass-border);
  background: var(--qr-glass-bg);
  color: var(--qr-starlight);
  font-size: 13px;
  outline: none;
  color-scheme: dark;
}

.qr-scanner-optimized .date-input-smart:focus,
.qr-scanner-optimized .activity-filter-smart:focus {
  border-color: var(--qr-candle-gold);
}

/* ============================================
   STATS
   ============================================ */

.qr-scanner-optimized .stats-compact {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 10px;
}

.qr-scanner-optimized .stat-mini {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 12px;
  border-radius: var(--qr-radius-md);
  background: var(--qr-glass-bg);
  border: 1px solid var(--qr-glass-border);
}

.qr-scanner-optimized .stat-mini-icon {
  font-size: 22px;
}

.qr-scanner-optimized .stat-mini-value {
  font-size: 20px;
  font-weight: 800;
  line-height: 1;
}

.qr-scanner-optimized .stat-mini-label {
  font-size: 11px;
  color: var(--qr-starlight-dim);
  margin-top: 2px;
}

.qr-scanner-optimized .stat-mini.green .stat-mini-value { color: var(--qr-emerald); }
.qr-scanner-optimized .stat-mini.purple .stat-mini-value { color: var(--qr-violet); }
.qr-scanner-optimized .stat-mini.blue .stat-mini-value { color: var(--qr-sky); }
.qr-scanner-optimized .stat-mini.orange .stat-mini-value { color: var(--qr-candle-gold); }

/* ============================================
   ATTENDANCE LIST
   ============================================ */

.qr-scanner-optimized .attendance-list-smart {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.qr-scanner-optimized .attendance-item-smart {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 10px 12px;
  border-radius: var(--qr-radius-md);
  background: var(--qr-glass-bg);
  border: 1px solid var(--qr-glass-border);
}

.qr-scanner-optimized .attendance-icon-smart {
  font-size: 22px;
  flex-shrink: 0;
}

.qr-scanner-optimized .attendance-details-smart {
  flex: 1;
  min-width: 0;
}

.qr-scanner-optimized .attendance-name-smart {
  font-size: 14px;
  font-weight: 600;
  color: var(--qr-starlight);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.qr-scanner-optimized .attendance-meta-smart {
  font-size: 12px;
  color: var(--qr-starlight-dim);
}

.qr-scanner-optimized .attendance-coins-smart {
  font-size: 13px;
  font-weight: 700;
  color: var(--qr-candle-gold);
  flex-shrink: 0;
}

.qr-scanner-optimized .empty-smart {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8px;
  padding: 32px 16px;
  color: var(--qr-starlight-dim);
  text-align: center;
}

.qr-scanner-optimized .empty-emoji {
  font-size: 36px;
}

.qr-scanner-optimized .loading-screen {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12px;
  padding: 32px 16px;
}

/* ============================================
   SUCCESS MODAL
   ============================================ */

.qr-scanner-optimized .modal-overlay-smart {
  position: fixed;
  inset: 0;
  z-index: 100;
  background: rgba(6, 8, 21, 0.75);
  backdrop-filter: blur(6px);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 20px;
}

.qr-scanner-optimized .modal-content-smart {
  width: 100%;
  max-width: 380px;
  background: var(--qr-midnight-light);
  border: 1px solid var(--qr-glass-border);
  border-radius: var(--qr-radius-lg);
  padding: 28px 22px;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
  animation: qr-modal-in 0.3s ease;
}

@keyframes qr-modal-in {
  from { opacity: 0; transform: scale(0.92) translateY(10px); }
  to { opacity: 1; transform: scale(1) translateY(0); }
}

.qr-scanner-optimized .success-modal-smart {
  text-align: center;
}

.qr-scanner-optimized .success-icon-smart {
  font-size: 48px;
  margin-bottom: 8px;
}

.qr-scanner-optimized .success-modal-smart h2 {
  font-size: 20px;
  margin-bottom: 18px;
}

.qr-scanner-optimized .success-details-smart {
  display: flex;
  flex-direction: column;
  gap: 10px;
  margin-bottom: 20px;
}

.qr-scanner-optimized .success-item-smart {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 10px 14px;
  border-radius: var(--qr-radius-sm);
  background: var(--qr-glass-bg);
  font-size: 14px;
}

.qr-scanner-optimized .success-item-smart.highlight {
  background: rgba(240, 180, 41, 0.15);
  border: 1px solid rgba(240, 180, 41, 0.3);
}

.qr-scanner-optimized .success-item-smart.total {
  background: linear-gradient(135deg, rgba(240, 180, 41, 0.2), rgba(255, 140, 66, 0.15));
  border: 1px solid rgba(240, 180, 41, 0.35);
  font-weight: 700;
}

.qr-scanner-optimized .success-label-smart {
  color: var(--qr-starlight-dim);
}

.qr-scanner-optimized .success-value-smart {
  font-weight: 700;
  color: var(--qr-starlight);
}

.qr-scanner-optimized .success-actions-smart {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.qr-scanner-optimized .btn-submit-smart {
  padding: 13px;
  border-radius: var(--qr-radius-md);
  border: none;
  background: linear-gradient(135deg, var(--qr-candle-gold), var(--qr-ember));
  color: var(--qr-midnight-deep);
  font-weight: 700;
  font-size: 14px;
  cursor: pointer;
  transition: transform 0.2s;
}

.qr-scanner-optimized .btn-submit-smart:hover {
  transform: translateY(-2px);
}

/* ============================================
   RESPONSIVE
   ============================================ */

@media (min-width: 480px) {
  .qr-scanner-optimized .smart-header,
  .qr-scanner-optimized .dashboard-body-smart {
    max-width: 480px;
    margin-inline: auto;
  }
}
