/* ============================================
   QRScanner.css
   تصميم: "سماء ليل الكنيسة" - خلفية ليلية متحركة
   (نجوم بتلمع + شرر/بخور صاعد) بدل الخلفية العادية،
   مع لمسة دهبية (لون الشمعة والعملة) كلون مميز
   ============================================ */

@import url('https://fonts.googleapis.com/css2?family=Aref+Ruqaa:wght@400;700&family=Cairo:wght@400;500;600;700;800&display=swap');

/* ============================================
   ROOT / VARIABLES (محصورة جوه الصفحة عشان متأثرش على صفحات تانية)
   ============================================ */

.qr-scanner-optimized {
  --midnight: #0B1026;
  --midnight-deep: #060815;
  --midnight-light: #161B3A;
  --candle-gold: #F0B429;
  --ember: #FF8C42;
  --starlight: #E7ECFA;
  --starlight-dim: rgba(231, 236, 250, 0.65);
  --glass-bg: rgba(255, 255, 255, 0.06);
  --glass-bg-strong: rgba(255, 255, 255, 0.1);
  --glass-border: rgba(255, 255, 255, 0.14);
  --violet: #A78BFA;
  --emerald: #34D399;
  --sky: #60A5FA;
  --radius-lg: 20px;
  --radius-md: 14px;
  --radius-sm: 10px;
  --shadow-glow: 0 8px 32px rgba(0, 0, 0, 0.4);

  position: relative;
  min-height: 100vh;
  background: var(--midnight);
  color: var(--starlight);
  font-family: 'Cairo', 'Tajawal', sans-serif;
  overflow-x: hidden;
  padding-bottom: 40px;
}

.qr-scanner-optimized * {
  box-sizing: border-box;
}

.qr-scanner-optimized h1,
.qr-scanner-optimized h2,
.qr-scanner-optimized h3 {
  font-family: 'Aref Ruqaa', 'Cairo', serif;
  margin: 0;
}

.qr-scanner-optimized button,
.qr-scanner-optimized input,
.qr-scanner-optimized select {
  font-family: inherit;
}

.qr-scanner-optimized button:focus-visible,
.qr-scanner-optimized input:focus-visible,
.qr-scanner-optimized select:focus-visible {
  outline: 2px solid var(--candle-gold);
  outline-offset: 2px;
}

/* ============================================
   الخلفية المتحركة - نجوم + شرر صاعد
   ============================================ */

.stars {
  position: fixed;
  inset: 0;
  z-index: 0;
  pointer-events: none;
  overflow: hidden;
  background:
    radial-gradient(ellipse 120% 60% at 50% -10%, rgba(167, 139, 250, 0.18), transparent 60%),
    linear-gradient(180deg, var(--midnight-deep) 0%, var(--midnight) 45%, #0E1330 100%);
}

/* طبقة النجوم - بعيدة وبطيئة */
.stars::before {
  content: '';
  position: absolute;
  inset: -10%;
  background-image:
    radial-gradient(1.5px 1.5px at 20px 30px, #E7ECFA, transparent),
    radial-gradient(1.5px 1.5px at 90px 120px, #E7ECFA, transparent),
    radial-gradient(1px 1px at 160px 60px, #E7ECFA, transparent),
    radial-gradient(1.5px 1.5px at 210px 180px, #E7ECFA, transparent),
    radial-gradient(1px 1px at 260px 40px, #E7ECFA, transparent),
    radial-gradient(2px 2px at 320px 140px, #E7ECFA, transparent),
    radial-gradient(1px 1px at 30px 200px, #E7ECFA, transparent),
    radial-gradient(1.5px 1.5px at 130px 240px, #E7ECFA, transparent);
  background-repeat: repeat;
  background-size: 360px 280px;
  opacity: 0.7;
  animation: stars-drift 90s linear infinite, stars-twinkle 4s ease-in-out infinite alternate;
}

/* طبقة الشرر/البخور الصاعد - قريبة ودافية */
.stars::after {
  content: '';
  position: absolute;
  inset: 0;
  background-image:
    radial-gradient(3px 3px at 10% 90%, rgba(240, 180, 41, 0.9), transparent 60%),
    radial-gradient(2.5px 2.5px at 25% 95%, rgba(255, 140, 66, 0.8), transparent 60%),
    radial-gradient(3px 3px at 45% 100%, rgba(240, 180, 41, 0.7), transparent 60%),
    radial-gradient(2px 2px at 65% 92%, rgba(255, 140, 66, 0.8), transparent 60%),
    radial-gradient(3px 3px at 80% 98%, rgba(240, 180, 41, 0.9), transparent 60%),
    radial-gradient(2.5px 2.5px at 95% 90%, rgba(255, 140, 66, 0.7), transparent 60%);
  filter: blur(0.5px);
  animation: embers-rise 12s ease-in infinite;
  opacity: 0.85;
}

@keyframes stars-drift {
  from { transform: translate3d(0, 0, 0); }
  to { transform: translate3d(-360px, -280px, 0); }
}

@keyframes stars-twinkle {
  from { opacity: 0.45; }
  to { opacity: 0.85; }
}

@keyframes embers-rise {
  0% { transform: translateY(0) scale(1); opacity: 0; }
  10% { opacity: 1; }
  90% { opacity: 0.6; }
  100% { transform: translateY(-100vh) scale(0.6); opacity: 0; }
}

@media (prefers-reduced-motion: reduce) {
  .stars::before,
  .stars::after,
  .header-glass::before {
    animation: none;
  }
}

/* ============================================
   HEADER
   ============================================ */

.smart-header {
  position: relative;
  z-index: 1;
  padding: 20px 16px 12px;
}

.header-glass {
  position: relative;
  overflow: hidden;
  background: var(--glass-bg);
  border: 1px solid var(--glass-border);
  border-radius: var(--radius-lg);
  padding: 18px 20px;
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  box-shadow: var(--shadow-glow);
}

.header-glass::before {
  content: '';
  position: absolute;
  top: -50%;
  inset-inline-end: -20%;
  width: 200px;
  height: 200px;
  background: radial-gradient(circle, rgba(240, 180, 41, 0.25), transparent 70%);
  animation: halo-pulse 6s ease-in-out infinite;
  pointer-events: none;
}

@keyframes halo-pulse {
  0%, 100% { opacity: 0.5; transform: scale(1); }
  50% { opacity: 1; transform: scale(1.15); }
}

.header-row {
  position: relative;
  z-index: 1;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
}

.greeting-section {
  flex: 1;
  min-width: 0;
}

.greeting-section h1 {
  font-size: 21px;
  font-weight: 700;
  color: var(--starlight);
  letter-spacing: 0.3px;
}

.status-row {
  margin-top: 6px;
}

.connection-status {
  font-size: 12px;
  color: var(--starlight-dim);
}

.connection-status.online {
  color: var(--emerald);
}

.header-actions {
  display: flex;
  align-items: center;
  gap: 8px;
}

/* ============================================
   أزرار عامة (رجوع، تحديث..)
   ============================================ */

.action-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 40px;
  height: 40px;
  border-radius: 50%;
  border: 1px solid var(--glass-border);
  background: var(--glass-bg);
  color: var(--starlight);
  font-size: 18px;
  cursor: pointer;
  transition: background 0.2s, transform 0.2s;
}

.action-btn:hover:not(:disabled) {
  background: var(--glass-bg-strong);
  transform: scale(1.05);
}

.action-btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.refresh-btn {
  width: 36px;
  height: 36px;
  font-size: 15px;
}

.refresh-btn:hover:not(:disabled) {
  transform: rotate(180deg);
}

/* ============================================
   TOAST
   ============================================ */

.toast-smart {
  position: fixed;
  top: 16px;
  left: 50%;
  transform: translateX(-50%);
  z-index: 100;
  padding: 12px 20px;
  border-radius: var(--radius-md);
  background: var(--midnight-light);
  border: 1px solid var(--glass-border);
  color: var(--starlight);
  box-shadow: var(--shadow-glow);
  font-size: 14px;
  max-width: 90%;
  text-align: center;
  white-space: pre-line;
  animation: toast-in 0.3s ease;
}

.toast-smart.error {
  border-color: rgba(248, 113, 113, 0.5);
  background: linear-gradient(135deg, rgba(127, 29, 29, 0.9), var(--midnight-light));
}

.toast-smart.success {
  border-color: rgba(52, 211, 153, 0.5);
  background: linear-gradient(135deg, rgba(6, 78, 59, 0.9), var(--midnight-light));
}

@keyframes toast-in {
  from { opacity: 0; transform: translate(-50%, -12px); }
  to { opacity: 1; transform: translate(-50%, 0); }
}

/* ============================================
   PROCESSING OVERLAY
   ============================================ */

.processing-overlay-smart {
  position: fixed;
  inset: 0;
  z-index: 90;
  background: rgba(6, 8, 21, 0.85);
  backdrop-filter: blur(6px);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 16px;
}

.logo-loader {
  position: relative;
  width: 64px;
  height: 64px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.logo-icon {
  font-size: 28px;
  z-index: 1;
}

.logo-spinner {
  position: absolute;
  inset: 0;
  border: 3px solid rgba(240, 180, 41, 0.2);
  border-top-color: var(--candle-gold);
  border-radius: 50%;
  animation: spin 0.9s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

.loading-text {
  color: var(--starlight-dim);
  font-size: 14px;
}

/* ============================================
   DASHBOARD BODY / SECTIONS
   ============================================ */

.dashboard-body-smart {
  position: relative;
  z-index: 1;
  padding: 8px 16px 24px;
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.section-smart {
  background: var(--glass-bg);
  border: 1px solid var(--glass-border);
  border-radius: var(--radius-lg);
  padding: 18px;
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
}

.section-title-smart {
  font-size: 16px;
  font-weight: 700;
  color: var(--starlight);
  margin-bottom: 14px;
}

.section-header-smart {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 14px;
}

.section-header-smart .section-title-smart {
  margin-bottom: 0;
}

/* ============================================
   ACTIVITY GRID
   ============================================ */

.activity-grid-smart {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 10px;
}

.activity-card-smart {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6px;
  background: var(--glass-bg);
  border: 1px solid var(--glass-border);
  border-radius: var(--radius-md);
  padding: 16px 8px;
  color: var(--starlight);
  cursor: pointer;
  transition: transform 0.2s, border-color 0.2s, background 0.2s;
}

.activity-card-smart:hover:not(:disabled) {
  transform: translateY(-3px);
  border-color: var(--candle-gold);
  background: var(--glass-bg-strong);
}

.activity-card-smart:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.activity-icon-smart {
  font-size: 28px;
}

.activity-card-smart h3 {
  font-size: 13px;
  font-weight: 600;
  text-align: center;
}

.activity-reward-smart {
  font-size: 12px;
  font-weight: 700;
  color: var(--candle-gold);
}

/* ============================================
   SCANNER SECTION
   ============================================ */

.scanner-section-smart {
  background: var(--glass-bg);
  border: 1px solid var(--glass-border);
  border-radius: var(--radius-lg);
  padding: 18px;
  backdrop-filter: blur(16px);
}

.scanner-header-smart {
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 8px;
  margin-bottom: 14px;
}

.scanner-header-smart h2 {
  font-size: 16px;
  font-weight: 700;
}

.cache-info {
  display: flex;
  align-items: center;
  gap: 6px;
  background: rgba(240, 180, 41, 0.15);
  border: 1px solid rgba(240, 180, 41, 0.3);
  padding: 6px 12px;
  border-radius: 999px;
}

.info-icon {
  font-size: 14px;
}

.info-text {
  font-size: 13px;
  font-weight: 600;
  color: var(--candle-gold);
}

.qr-reader-smart {
  border-radius: var(--radius-md);
  overflow: hidden;
  margin-bottom: 14px;
}

.btn-cancel-smart {
  width: 100%;
  padding: 12px;
  border-radius: var(--radius-md);
  border: 1px solid rgba(248, 113, 113, 0.35);
  background: rgba(127, 29, 29, 0.25);
  color: #FCA5A5;
  font-weight: 600;
  font-size: 14px;
  cursor: pointer;
  transition: background 0.2s;
}

.btn-cancel-smart:hover:not(:disabled) {
  background: rgba(127, 29, 29, 0.4);
}

.btn-cancel-smart:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

/* ============================================
   DATE BROWSER (اعرض بيانات يوم)
   ============================================ */

.date-browser-section-smart {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.date-chips-smart {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.chip-smart {
  padding: 8px 16px;
  border-radius: 999px;
  border: 1px solid var(--glass-border);
  background: var(--glass-bg);
  color: var(--starlight-dim);
  font-size: 13px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s ease;
  white-space: nowrap;
}

.chip-smart:hover {
  background: var(--glass-bg-strong);
  color: var(--starlight);
}

.chip-smart.active {
  background: linear-gradient(135deg, var(--candle-gold), var(--ember));
  border-color: transparent;
  color: var(--midnight-deep);
  box-shadow: 0 2px 14px rgba(240, 180, 41, 0.45);
}

.date-picker-row-smart {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
}

.date-input-smart,
.activity-filter-smart {
  flex: 1;
  min-width: 140px;
  padding: 10px 12px;
  border-radius: var(--radius-sm);
  border: 1px solid var(--glass-border);
  background: var(--glass-bg);
  color: var(--starlight);
  font-size: 13px;
  outline: none;
  color-scheme: dark;
}

.date-input-smart:focus,
.activity-filter-smart:focus {
  border-color: var(--candle-gold);
}

/* ============================================
   STATS
   ============================================ */

.stats-compact {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 10px;
}

.stat-mini {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 12px;
  border-radius: var(--radius-md);
  background: var(--glass-bg);
  border: 1px solid var(--glass-border);
}

.stat-mini-icon {
  font-size: 22px;
}

.stat-mini-value {
  font-size: 20px;
  font-weight: 800;
  line-height: 1;
}

.stat-mini-label {
  font-size: 11px;
  color: var(--starlight-dim);
  margin-top: 2px;
}

.stat-mini.green .stat-mini-value { color: var(--emerald); }
.stat-mini.purple .stat-mini-value { color: var(--violet); }
.stat-mini.blue .stat-mini-value { color: var(--sky); }
.stat-mini.orange .stat-mini-value { color: var(--candle-gold); }

/* ============================================
   ATTENDANCE LIST
   ============================================ */

.attendance-list-smart {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.attendance-item-smart {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 10px 12px;
  border-radius: var(--radius-md);
  background: var(--glass-bg);
  border: 1px solid var(--glass-border);
}

.attendance-icon-smart {
  font-size: 22px;
  flex-shrink: 0;
}

.attendance-details-smart {
  flex: 1;
  min-width: 0;
}

.attendance-name-smart {
  font-size: 14px;
  font-weight: 600;
  color: var(--starlight);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.attendance-meta-smart {
  font-size: 12px;
  color: var(--starlight-dim);
}

.attendance-coins-smart {
  font-size: 13px;
  font-weight: 700;
  color: var(--candle-gold);
  flex-shrink: 0;
}

.empty-smart {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8px;
  padding: 32px 16px;
  color: var(--starlight-dim);
  text-align: center;
}

.empty-emoji {
  font-size: 36px;
}

.loading-screen {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12px;
  padding: 32px 16px;
}

/* ============================================
   SUCCESS MODAL
   ============================================ */

.modal-overlay-smart {
  position: fixed;
  inset: 0;
  z-index: 100;
  background: rgba(6, 8, 21, 0.75);
  backdrop-filter: blur(6px);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 20px;
}

.modal-content-smart {
  width: 100%;
  max-width: 380px;
  background: var(--midnight-light);
  border: 1px solid var(--glass-border);
  border-radius: var(--radius-lg);
  padding: 28px 22px;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
  animation: modal-in 0.3s ease;
}

@keyframes modal-in {
  from { opacity: 0; transform: scale(0.92) translateY(10px); }
  to { opacity: 1; transform: scale(1) translateY(0); }
}

.success-modal-smart {
  text-align: center;
}

.success-icon-smart {
  font-size: 48px;
  margin-bottom: 8px;
}

.success-modal-smart h2 {
  font-size: 20px;
  margin-bottom: 18px;
}

.success-details-smart {
  display: flex;
  flex-direction: column;
  gap: 10px;
  margin-bottom: 20px;
}

.success-item-smart {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 10px 14px;
  border-radius: var(--radius-sm);
  background: var(--glass-bg);
  font-size: 14px;
}

.success-item-smart.highlight {
  background: rgba(240, 180, 41, 0.15);
  border: 1px solid rgba(240, 180, 41, 0.3);
}

.success-item-smart.total {
  background: linear-gradient(135deg, rgba(240, 180, 41, 0.2), rgba(255, 140, 66, 0.15));
  border: 1px solid rgba(240, 180, 41, 0.35);
  font-weight: 700;
}

.success-label-smart {
  color: var(--starlight-dim);
}

.success-value-smart {
  font-weight: 700;
  color: var(--starlight);
}

.success-actions-smart {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.btn-submit-smart {
  padding: 13px;
  border-radius: var(--radius-md);
  border: none;
  background: linear-gradient(135deg, var(--candle-gold), var(--ember));
  color: var(--midnight-deep);
  font-weight: 700;
  font-size: 14px;
  cursor: pointer;
  transition: transform 0.2s;
}

.btn-submit-smart:hover {
  transform: translateY(-2px);
}

/* ============================================
   RESPONSIVE - يوسع شوية على شاشات أكبر من الموبايل
   ============================================ */

@media (min-width: 480px) {
  .smart-header,
  .dashboard-body-smart {
    max-width: 480px;
    margin-inline: auto;
  }
}
